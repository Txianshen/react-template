// // 智能体管理
// POST / api / agents / { agent_type } / start;
// POST / api / agents / { agent_type } / stop;
// GET / api / agents / { agent_type } / status;

// // 聊天交互（SSE 流式）
// POST / api / agents / { agent_type } / chat;

// // 历史记录（仅综合智能体）
// GET / api / agents / general / history;
// POST / api / agents / general / history / load;

// // 报告下载（仅综合智能体）
// GET / api / reports;
// GET / api / reports / { filename };
